[project]
name = "java-mocha"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.3",
    "requests>=2.32.4",
    "typer>=0.16.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.14.1",
]
