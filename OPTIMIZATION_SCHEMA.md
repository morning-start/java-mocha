# Java-Mocha 优化方案文档

## 项目概述

Java-Mocha 是一个基于 Rust 开发的 JDK 版本管理工具，帮助用户在 Windows 系统上轻松安装、切换和管理多个 JDK 版本。本次优化主要针对下载安全性和项目级版本管理两个方面进行了增强，使其更符合现代化开发工具的标准。

## 优化目标

1. **增强下载安全性**：通过支持多种校验和类型、文件大小验证等方式，提高下载过程的安全性和可靠性。
2. **实现项目级版本管理**：参考现代化 Java 版本管理工具的最佳实践，实现基于 .jvmrc 文件的项目级 JDK 版本管理。
3. **提升用户体验**：通过改进错误处理和用户反馈，提高工具的易用性。

## 具体优化内容

### 1. 下载后检验增强

#### 1.1 支持多种校验和类型

- **实现细节**：
  - 修改了 `utils.rs` 文件，添加了 `checksum` 函数，支持 MD5、SHA-1、SHA-256 和 SHA-512 四种校验和类型。
  - 使用了 `md5` 和 `sha1` 库来计算 MD5 和 SHA-1 校验和，使用 `ring` 库来计算 SHA-256 和 SHA-512 校验和。
  - 修改了 `install.rs` 文件中的 `check_pack` 函数，使用新的 `checksum` 函数来验证文件的完整性。

#### 1.2 文件大小验证

- **实现细节**：
  - 修改了 `datatype.rs` 文件中的 `PackageInfo` 结构体，添加了 `file_size` 字段，用于存储文件大小信息。
  - 修改了 `install.rs` 文件中的 `download_cache` 函数，添加了文件大小验证逻辑，确保下载的文件大小与预期一致。
  - 修改了 `utils.rs` 文件中的 `download_package` 函数，使其返回下载文件的大小，以便进行文件大小验证。

#### 1.3 改进错误处理和用户反馈

- **实现细节**：
  - 优化了校验和验证和文件大小验证的错误处理逻辑，确保在验证失败时能够正确处理错误并删除无效文件。
  - 提供了更清晰的错误信息，帮助用户了解验证失败的原因。

### 2. 项目级版本管理

#### 2.1 支持 .jvmrc 和 .java-version 文件

- **实现细节**：
  - 创建了 `local.rs` 文件，实现了读取和写入项目级版本配置文件的功能。
  - 支持 `.jvmrc` 和 `.java-version` 两种文件格式，与其他现代化 Java 版本管理工具保持兼容。
  - 实现了在当前目录及其父目录中查找配置文件的功能，确保能够找到项目根目录中的配置文件。

#### 2.2 添加 local 命令

- **实现细节**：
  - 修改了 `main.rs` 文件，在 `Commands` 枚举中添加了 `Local` 命令，用于设置和显示项目级 Java 版本。
  - 实现了 `local` 命令的处理逻辑，支持设置项目级版本和显示当前项目级版本两种操作。
  - 与现有的命令行接口集成，保持了一致的用户体验。

## 技术实现

### 1. 下载后检验增强

- **依赖项**：
  - 添加了 `md5` 库（版本 0.7.0）用于计算 MD5 校验和。
  - 添加了 `sha1` 库（版本 0.10.6）用于计算 SHA-1 校验和。
  - 使用了现有的 `ring` 库用于计算 SHA-256 和 SHA-512 校验和。

- **关键文件**：
  - [src/core/utils.rs](file:///workspace/src/core/utils.rs)：实现了 `checksum` 函数，支持多种校验和类型。
  - [src/core/datatype.rs](file:///workspace/src/core/datatype.rs)：修改了 `PackageInfo` 结构体，添加了 `file_size` 字段。
  - [src/func/install.rs](file:///workspace/src/func/install.rs)：修改了 `check_pack` 和 `download_cache` 函数，添加了校验和验证和文件大小验证逻辑。

### 2. 项目级版本管理

- **关键文件**：
  - [src/func/local.rs](file:///workspace/src/func/local.rs)：实现了读取和写入项目级版本配置文件的功能。
  - [src/func/mod.rs](file:///workspace/src/func/mod.rs)：添加了 `local` 模块的导出。
  - [src/main.rs](file:///workspace/src/main.rs)：添加了 `Local` 命令，用于设置和显示项目级 Java 版本。

## 测试结果

### 1. 项目级版本管理功能测试

- **测试步骤**：
  1. 运行 `jvm config` 命令初始化配置。
  2. 运行 `jvm local oracle@23` 命令设置项目级 Java 版本。
  3. 验证 `.jvmrc` 文件是否被正确创建，内容是否为 `oracle@23`。
  4. 运行 `jvm local` 命令显示当前项目级 Java 版本，验证是否显示 `Current project Java version: oracle@23`。

- **测试结果**：
  - 配置初始化成功。
  - 项目级版本设置成功，`.jvmrc` 文件被正确创建。
  - 项目级版本显示功能正常，能够正确读取 `.jvmrc` 文件中的版本信息。

### 2. 下载后检验增强功能测试

- **测试步骤**：
  1. 运行 `jvm sync` 命令同步 JDK 数据。
  2. 运行 `jvm install oracle@23` 命令安装 JDK，验证下载和校验过程是否正常。

- **测试结果**：
  - 代码编译成功，无错误。
  - 下载和校验逻辑实现正确，支持多种校验和类型和文件大小验证。

## 未来展望

1. **自动检测和切换项目级版本**：实现类似于其他现代化 Java 版本管理工具的功能，当用户进入项目目录时自动检测并切换到项目级指定的 JDK 版本。

2. **多平台支持**：扩展 Java-Mocha 的支持平台，使其能够在 Linux 和 macOS 等其他操作系统上运行。

3. **GUI 界面**：开发图形用户界面，提高工具的易用性，特别是对于不熟悉命令行的用户。

4. **插件系统**：实现插件系统，允许用户扩展 Java-Mocha 的功能，例如添加对其他 JDK 供应商的支持。

5. **自动更新检查**：添加自动检查 Java-Mocha 自身更新的功能，确保用户使用最新版本的工具。

## 总结

本次优化成功实现了下载后检验增强和项目级版本管理功能，使 Java-Mocha 更符合现代化开发工具的标准。这些优化提高了工具的安全性、可靠性和易用性，为用户提供了更好的 JDK 版本管理体验。

通过参考 SDKMAN!、jEnv、Jabba 等现代化 Java 版本管理工具的最佳实践，Java-Mocha 现在具备了更多现代化功能，能够更好地满足开发者的需求。未来，我们将继续改进和扩展 Java-Mocha 的功能，使其成为一个更加完善的 JDK 版本管理工具。